import { RelativeToNowPipe } from './relative-to-now.pipe';

describe('RelativeToNowPipe', () => {
  it('create an instance', () => {
    const pipe = new RelativeToNowPipe();
    expect(pipe).toBeTruthy();
  });
});
